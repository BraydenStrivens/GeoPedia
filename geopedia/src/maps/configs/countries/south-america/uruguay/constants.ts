/**
 * Shared constants used by Uruguay's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Uruguay.
 */
export const URUGUAY_INITIAL_VIEW: MapInitialView = {
  center: [-56.0, -32.8],
  zoom: 5.5,
};
