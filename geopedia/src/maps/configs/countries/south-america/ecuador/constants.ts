/**
 * Shared constants used by Ecuador's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by Ecuador's maps.
 */
export const ECUADOR_INITIAL_VIEW: MapInitialView = {
  center: [-83.5, -1.4],
  zoom: 5,
};
