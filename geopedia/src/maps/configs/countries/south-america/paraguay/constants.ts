/**
 * Shared constants used by Paraguay's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Paraguay.
 */
export const PARAGUAY_INITIAL_VIEW: MapInitialView = {
  center: [-58.3, -23.4],
  zoom: 4.9,
};
