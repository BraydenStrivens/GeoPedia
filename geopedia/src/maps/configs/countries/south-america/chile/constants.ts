/**
 * Shared constants used by Chile's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Chile.
 */
export const CHILE_INITIAL_VIEW: MapInitialView = {
  center: [-71.5, -37.5],
  zoom: 3.2,
};
