/**
 * Shared constants used by Colombia's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Colombia.
 */
export const COLOMBIA_INITIAL_VIEW: MapInitialView = {
  center: [-74.3, 4.5],
  zoom: 4.6,
};
