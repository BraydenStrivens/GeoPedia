/**
 * Shared constants used by Argentina's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Argentina.
 */
export const ARGENTINA_INITIAL_VIEW: MapInitialView = {
  center: [-64.5, -38.5],
  zoom: 3.4,
};
