/**
 * Shared constants used by Brazil's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Brazil.
 */
export const BRAZIL_INITIAL_VIEW: MapInitialView = {
  center: [-52.5, -14.5],
  zoom: 3.2,
};
