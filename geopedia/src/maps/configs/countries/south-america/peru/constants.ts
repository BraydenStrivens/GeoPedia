/**
 * Shared constants used by Peru's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Peru.
 */
export const PERU_INITIAL_VIEW: MapInitialView = {
  center: [-75.2, -9.3],
  zoom: 4.8,
};
