/**
 * Shared constants used by Bolivia's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Bolivia.
 */
export const BOLIVIA_INITIAL_VIEW: MapInitialView = {
  center: [-64.7, -16.7],
  zoom: 4.6,
};
