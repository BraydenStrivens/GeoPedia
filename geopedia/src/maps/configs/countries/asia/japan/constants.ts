/**
 * Shared map constants for Japan's geography quizzes.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial map view used by Japan's geography quizzes.
 */
export const JAPAN_INITIAL_VIEW: MapInitialView = {
  center: [137.5, 36.2],
  zoom: 4.2,
};
