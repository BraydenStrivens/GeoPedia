/**
 * Shared map constants for South Korea's geography quizzes.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial map view used by South Korea's geography quizzes.
 */
export const SOUTH_KOREA_INITIAL_VIEW: MapInitialView = {
  center: [127.8, 36.3],
  zoom: 5.6,
};
