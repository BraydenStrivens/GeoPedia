/**
 * Shared constants used by Costa Rica's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Canada.
 */
export const COSTA_RICA_INITIAL_VIEW: MapInitialView = {
  center: [-84.1, 9.8],
  zoom: 6.5,
};
