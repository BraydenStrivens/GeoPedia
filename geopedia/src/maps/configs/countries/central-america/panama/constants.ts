/**
 * Shared constants used by the Panama's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Panama.
 */
export const PANAMA_INITIAL_VIEW: MapInitialView = {
  center: [-80.5, 8.5],
  zoom: 6.5,
};
