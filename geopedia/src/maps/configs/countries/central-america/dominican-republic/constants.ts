/**
 * Shared constants used by the Dominican Republic's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering the Dominican Republic.
 */
export const DOMINICAN_REPUBLIC_INITIAL_VIEW: MapInitialView = {
  center: [-70.4, 18.8],
  zoom: 7,
};
