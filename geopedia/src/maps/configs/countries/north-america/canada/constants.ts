/**
 * Shared constants used by Canada's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Canada.
 */
export const CANADA_INITIAL_VIEW: MapInitialView = {
  center: [-96, 61],
  zoom: 2.4,
};
