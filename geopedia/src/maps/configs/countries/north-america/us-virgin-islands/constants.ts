/**
 * Shared constants used by the US Virgin Islands's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering the US Virgin Islands.
 */
export const US_VIRGIN_ISLANDS_INITIAL_VIEW: MapInitialView = {
  center: [-64.78, 18.05],
  zoom: 9,
};
