/**
 * Shared constants used by the United States map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering the United States.
 */
export const USA_INITIAL_VIEW: MapInitialView = {
  center: [-98.5, 39.8],
  zoom: 3.5,
};
