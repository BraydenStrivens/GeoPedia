/**
 * Shared constants used by Mexico's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Mexico.
 */
export const MEXICO_INITIAL_VIEW: MapInitialView = {
  center: [-102, 23.5],
  zoom: 3.6,
};
