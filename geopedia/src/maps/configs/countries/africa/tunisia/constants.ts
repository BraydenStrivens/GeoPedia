/**
 * Shared constants used by Tunisia's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Tunisia.
 */
export const TUNISIA_INITIAL_VIEW: MapInitialView = {
  center: [9.5, 34],
  zoom: 5.7,
};
