/**
 * Shared constants for Rwanda's geography quizzes.
 */

import type { MapInitialView } from "@/maps/types";

export const RWANDA_INITIAL_VIEW: MapInitialView = {
  center: [29.9, -2.0],
  zoom: 7.6,
};
