/**
 * Shared constants for Namibia's geography quizzes.
 */

import type { MapInitialView } from "@/maps/types";

export const NAMIBIA_INITIAL_VIEW: MapInitialView = {
  center: [17.5, -22.0],
  zoom: 4.5,
};
