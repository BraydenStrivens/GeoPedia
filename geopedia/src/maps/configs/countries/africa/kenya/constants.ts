/**
 * Shared map constants for Kenya's geography quizzes.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial map view used by Kenya's geography quizzes.
 */
export const KENYA_INITIAL_VIEW: MapInitialView = {
  center: [37.8, 0.2],
  zoom: 5.5,
};
