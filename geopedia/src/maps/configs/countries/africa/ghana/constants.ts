/**
 * Shared constants used by Ghana's map configurations.
 */

import type { MapInitialView } from "@/maps/types";

/**
 * Initial camera position shared by maps covering Ghana.
 */
export const GHANA_INITIAL_VIEW: MapInitialView = {
  center: [-1.2, 7.9],
  zoom: 6,
};
