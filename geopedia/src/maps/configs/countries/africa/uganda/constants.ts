/**
 * Shared constants for Uganda's geography quizzes.
 */

import type { MapInitialView } from "@/maps/types";

export const UGANDA_INITIAL_VIEW: MapInitialView = {
  center: [32.3, 1.4],
  zoom: 6.3,
};
